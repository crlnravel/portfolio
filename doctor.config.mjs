export default {
	ignore: {
		// The starter component was intentionally removed, but remains in the
		// Git index until the deletion is committed. React Doctor includes
		// indexed paths in its full scan and otherwise tries to read this path.
		files: ['doctor.config.mjs', 'src/components/Welcome.astro']
	}
};
